# Deterministic scheduler order trace

This executable foundation example advances the named clocks, executes two mock authoritative systems in state-dependency order, and records all thirteen frozen Runtime Scheduler phases. It uses the same public runtime contracts intended for later physics packages.
