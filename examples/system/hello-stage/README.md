# Hello Stage

The original repository-bootstrap example is now an executable current-runtime fixture. It constructs one deterministic shared render frame, sends it through the SVG adapter and verifies semantic picking metadata instead of remaining a documentation-only placeholder.

Run it with `pnpm vitest run tests/examples/hello-stage.test.ts`.