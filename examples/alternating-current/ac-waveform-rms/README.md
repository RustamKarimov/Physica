# AC waveform and RMS

**Question:** How does RMS relate a sinusoid to equivalent heating?

This Phase 11 Gallery fixture executes `runFieldScenario("ac-waveform-rms")` through the same deterministic `@physica/physics-fields` API used by the desktop workflow. Values are SI-canonical.

## Assumptions

- ideal sinusoid
- steady frequency

## Representations

- oscilloscope waveform
- RMS marker
- direction reversal

The checked-in SVG is an accessible deterministic preview. Final packaged `.physica`, PNG and WebM artifacts remain tracked for the shared Phase 20 export pipeline.
