# Two independent clocks

This executable foundation example starts the mandatory simulation and presentation clocks, pauses simulation time, continues presentation time, then scrubs simulation independently. It proves time-domain separation without claiming that a physics runtime, projectile, animation, or renderer already exists.

`pending-artifacts.json` records the richer shared-runtime project/video obligations for the later Runtime Scheduler and rendering phases.
