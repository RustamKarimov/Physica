# Projectile explanation

This example uses a deterministic vertical-velocity fixture to demonstrate storyboard orchestration. It intentionally does not claim that the Phase 6 projectile solver exists yet.
