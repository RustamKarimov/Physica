# Drag a prefab

Discover the reusable pulley-and-mass apparatus prefab and its registered creation definition.

This Step 10 gallery example is executable and checks its deterministic output plus the checked-in expected preview. Run it from the repository root with `pnpm vitest run examples/library/drag-prefab`.

