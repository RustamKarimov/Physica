# Drag a smart model

Discover a smart model and emit its versioned Library drag payload.

This Step 10 gallery example is executable and checks its deterministic output plus the checked-in expected preview. Run it from the repository root with `pnpm vitest run examples/library/drag-smart-model`.

