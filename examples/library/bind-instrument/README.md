# Bind an instrument

Inspect the stopwatch instrument contract and its time-observable binding intent.

This Step 10 gallery example is executable and checks its deterministic output plus the checked-in expected preview. Run it from the repository root with `pnpm vitest run examples/library/bind-instrument`.

