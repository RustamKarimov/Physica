# Foundation object pack

Verify the complete first-party mechanics, apparatus, instrument, and representation object set.

This Step 10 gallery example is executable and checks its deterministic output plus the checked-in expected preview. Run it from the repository root with `pnpm vitest run examples/library/foundation-object-pack`.

