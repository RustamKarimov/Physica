# Save to My Library

Show the user-owned Library source and canonical portable bundle contract.

This Step 10 gallery example is executable and checks its deterministic output plus the checked-in expected preview. Run it from the repository root with `pnpm vitest run examples/library/save-to-my-library`.

