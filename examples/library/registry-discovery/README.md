# Registry discovery

Query the four declarative registries through the deterministic Physics Library catalog.

This Step 10 gallery example is executable and checks its deterministic output plus the checked-in expected preview. Run it from the repository root with `pnpm vitest run examples/library/registry-discovery`.

