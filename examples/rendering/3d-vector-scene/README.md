# 3D vector scene

This executable example translates a known `(3, 4, 0)` vector into the Three adapter's immutable shaft-and-head plan. Its numerical baseline verifies length, endpoint, head geometry, and the projected semantic picking segment under the shared perspective camera.

The SVG preview describes the fixed projection; hardware-dependent WebGL pixels are not used as the only scientific assertion.
