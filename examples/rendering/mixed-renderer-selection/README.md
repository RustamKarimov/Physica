# Mixed renderer selection

This executable example gives the picking service three overlapping semantic regions in shuffled adapter order. It proves that layer priority—not callback order or backend-native objects—produces the stable result `SVG → Pixi → Three`.

The output contains only Physica semantic identifiers and renderer metadata.
