# Particle cloud

This executable example supplies six immutable world positions to the Pixi adapter. An explicit stride selects every second point, then viewport culling removes the out-of-frame point. The expected output proves selection order, projected centers, semantic pick-region count, and unchanged source state.

The SVG preview is an accessible deterministic description of the Pixi plan; browser/GPU raster capture remains registered as a pending gallery artifact.
