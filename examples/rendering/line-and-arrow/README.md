# Line and arrow

This executable example projects a world-space line and force-style arrow through the shared orthographic camera. The checked-in SVG is the adapter's exact deterministic output and proves that physical `+y` becomes screen-up through camera projection alone.

Run the example test from the repository root with `pnpm vitest run examples/rendering/line-and-arrow`.
