# Measure area without hiding uncertainty

For `v = 1 + 2t`, the piecewise-linear trapezoidal area from 0 to 4 seconds is exactly 20 metres. The same stored measurements produce an ordinary unweighted fit with gradient 2 m/s² and R² = 1, while supplied time and velocity uncertainties remain visible error bars rather than silent fit weights.
