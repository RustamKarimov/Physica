# Derive distributions and frequencies reproducibly

Eight stored observations are binned with explicit boundaries, including the final right edge. Separately, sixteen samples of a 2 Hz sine measured at 16 Hz are transformed by the deterministic direct real DFT into a one-sided amplitude spectrum with a unit peak at 2 Hz.

Both outputs are first-class derived datasets with source provenance; neither depends on render FPS.
