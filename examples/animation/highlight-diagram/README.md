# Highlight a diagram

This Step 12 example highlights the force vector while explicitly dimming the velocity vector. Emphasis is transient presentation state and the teaching meaning is also stated in text rather than color alone.

Run it with `pnpm vitest run examples/animation/highlight-diagram`.
