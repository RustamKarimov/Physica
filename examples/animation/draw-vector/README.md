# Draw a vector

This Step 12 example evaluates one path-draw effect at 0 s, 1 s and 2 s. The SVG plan uses the exact 100-pixel path length; its arrow head remains hidden until the shaft is complete.

Run it from the repository root with `pnpm vitest run examples/animation/draw-vector`.
