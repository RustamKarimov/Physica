# Long-solenoid field

**Question:** How do turns density and current set the interior field?

This Phase 11 Gallery fixture executes `runFieldScenario("solenoid-field")` through the same deterministic `@physica/physics-fields` API used by the desktop workflow. Values are SI-canonical.

## Assumptions

- long ideal air-core solenoid
- uniform interior field

## Representations

- solenoid
- current arrows
- field lines
- Hall probe

The checked-in SVG is an accessible deterministic preview. Final packaged `.physica`, PNG and WebM artifacts remain tracked for the shared Phase 20 export pipeline.
